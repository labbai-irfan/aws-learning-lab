# Copy to .env on the server and fill in real values:  cp .env.example .env
# Then secure it:  chmod 600 .env
NODE_ENV=production
PORT=5000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=todouser
DB_PASSWORD=ChangeMe_Strong!123
DB_NAME=tododb
