# Module 17 — 200 Advanced AWS MCQs

> Each question has exactly one correct answer. Answer key at the bottom of each section.

---

## Section A — CloudFront (Q1–20)

**Q1.** An ACM certificate for a CloudFront distribution must be in which region?
- A) The region closest to your users
- B) The same region as your origin
- C) us-east-1 (global)
- D) Any region where CloudFront has a PoP

**Q2.** Which CloudFront feature restricts S3 origin access so the bucket can remain private?
- A) OAI (Origin Access Identity)
- B) OAC (Origin Access Control) ✓
- C) Signed Cookies
- D) Field-Level Encryption

**Q3.** A React SPA's `index.html` should have which TTL in CloudFront?
- A) 31,536,000 seconds (1 year)
- B) 0 (no-cache) ✓
- C) 86,400 seconds (1 day)
- D) 3,600 seconds (1 hour)

**Q4.** Lambda@Edge can run at which trigger points? (Choose the MOST complete answer)
- A) Viewer request only
- B) Origin request and origin response only
- C) Viewer request, viewer response, origin request, origin response ✓
- D) Only during cache miss

**Q5.** CloudFront Functions differ from Lambda@Edge in that CloudFront Functions:
- A) Can read and write the request body
- B) Can make network calls to external services
- C) Execute only at viewer-request and viewer-response events ✓
- D) Support Python and Node.js runtimes

**Q6.** What does Origin Shield provide?
- A) DDoS protection at the edge
- B) An extra caching layer between PoPs and the origin to reduce origin load ✓
- C) SSL termination for custom origins
- D) IP whitelisting for origin servers

**Q7.** A cache policy's MinTTL is 60s and an origin returns `Cache-Control: max-age=10`. What TTL does CloudFront use?
- A) 10 seconds (origin wins)
- B) 60 seconds (MinTTL floor applies) ✓
- C) 35 seconds (average)
- D) 0 seconds (no-cache from origin)

**Q8.** Which metric is MOST useful for diagnosing a low CloudFront cache-hit ratio?
- A) OriginLatency
- B) BytesDownloaded
- C) CacheHitRate ✓
- D) 5xxErrorRate

**Q9.** To serve a React SPA on CloudFront with S3, returning `index.html` for unknown paths, you should:
- A) Configure custom error response: 404 → index.html with 200 ✓
- B) Enable website hosting on the S3 bucket and use it as the origin
- C) Use Lambda@Edge to redirect 404s
- D) Enable Origin Shield

**Q10.** CloudFront charges for invalidations. The first N paths per month are free. What is N?
- A) 100
- B) 500
- C) 1,000 ✓
- D) 10,000

**Q11.** Signed URLs vs Signed Cookies — signed cookies are preferred when:
- A) Protecting a single file
- B) Protecting multiple files in the same distribution ✓
- C) The client is a mobile app
- D) Using Lambda@Edge

**Q12.** How does CloudFront integrate with WAF?
- A) WAF is enabled inside the CloudFront distribution settings as a native feature
- B) A WAF Web ACL (us-east-1 global scope) is attached to the distribution ✓
- C) WAF runs at the origin and CloudFront forwards all requests
- D) WAF integrates with CloudFront only via AWS Shield

**Q13.** A distribution has a `Default (*)` behaviour and a `/api/*` behaviour. A request for `/api/users` will:
- A) Match the Default behaviour
- B) Match the `/api/*` behaviour ✓
- C) Return a 404
- D) Match both behaviours in sequence

**Q14.** What is a CloudFront origin group used for?
- A) Grouping multiple cache policies
- B) Load balancing between origins
- C) Providing failover from a primary to a secondary origin ✓
- D) Sharing a CloudFront distribution between multiple accounts

**Q15.** Field-Level Encryption is used to:
- A) Encrypt the CloudFront access logs
- B) Encrypt specific HTTP POST fields at the edge using a public key ✓
- C) Enforce HTTPS between CloudFront and the origin
- D) Encrypt cookies in the cache key

**Q16.** Which CloudFront feature allows safe testing of a new distribution configuration with a subset of traffic?
- A) A/B testing with Lambda@Edge
- B) Origin groups with weighted routing
- C) Continuous deployment with staging distributions ✓
- D) CloudFront real-time logs

**Q17.** A `Vary: Cookie` response header from the origin will:
- A) Improve cache hit rate by allowing cookie-based caching
- B) Prevent CloudFront from caching the response ✓
- C) Enable Lambda@Edge to read cookies
- D) Forward all cookies to the origin

**Q18.** CloudFront real-time logs differ from standard access logs in that they:
- A) Are cheaper to store
- B) Are sent to Kinesis Data Streams (near real-time) ✓
- C) Are stored in S3 automatically
- D) Only capture cache misses

**Q19.** To reduce origin load for a public API with some cacheable endpoints, you should:
- A) Set all TTLs to 0 and rely on the API Gateway cache
- B) Use separate behaviours for cacheable vs non-cacheable paths ✓
- C) Enable Origin Shield on all requests
- D) Use a WAF rate-based rule

**Q20.** A CloudFront distribution has `Compress objects automatically` enabled. Compression happens:
- A) At the origin before sending to CloudFront
- B) Only in Lambda@Edge
- C) At the PoP when serving the response to the client ✓
- D) Only for objects > 10 MB

**Section A answer key:** 1-C, 2-B, 3-B, 4-C, 5-C, 6-B, 7-B, 8-C, 9-A, 10-C, 11-B, 12-B, 13-B, 14-C, 15-B, 16-C, 17-B, 18-B, 19-B, 20-C

---

## Section B — ElastiCache & Redis (Q21–40)

**Q21.** Which Redis eviction policy should be used for a session store where no session should ever be silently dropped?
- A) allkeys-lru
- B) volatile-lru
- C) noeviction ✓
- D) allkeys-random

**Q22.** In Redis cluster mode, what error occurs when a pipeline contains keys in different hash slots?
- A) WRONGTYPE
- B) OOM
- C) CROSSSLOT ✓
- D) MOVED

**Q23.** How many hash slots does Redis cluster use?
- A) 1,024
- B) 4,096
- C) 8,192
- D) 16,384 ✓

**Q24.** A cache-aside pattern means:
- A) The cache layer automatically fetches missing data from the DB
- B) The application checks the cache first; on miss, fetches from DB and populates cache ✓
- C) Writes go to the cache only and are flushed asynchronously to DB
- D) Reads always go to DB with cache as a write buffer

**Q25.** Which CloudWatch metric indicates that Redis is running out of memory and dropping keys?
- A) CurrConnections
- B) ReplicationLag
- C) Evictions ✓
- D) CacheMisses

**Q26.** ElastiCache Serverless auto-scales based on:
- A) CPU utilisation of the underlying EC2 instances
- B) Cache usage (stored data + ECUs consumed) ✓
- C) Number of client connections
- D) Network bandwidth utilisation

**Q27.** A distributed lock implemented with Redis SET NX EX 10 MUST:
- A) Set a key only if it doesn't exist, with a 10-second expiry ✓
- B) Set the key and replace any existing value
- C) Check if the key exists and return the current TTL
- D) Delete the key after 10 operations

**Q28.** The Redlock algorithm acquires the lock on:
- A) A single Redis primary node
- B) At least N/2+1 independent Redis nodes ✓
- C) A Redis cluster's hash slot 0
- D) Any available replica

**Q29.** In cluster mode, which strategy ensures that `session:user42` and `profile:user42` land in the same hash slot?
- A) Use the same TTL for both keys
- B) Store them in the same Redis database (DB 0)
- C) Use hash tags: `{user42}:session` and `{user42}:profile` ✓
- D) Enable `cluster-announce-hostname`

**Q30.** Which eviction policy is BEST for a fully ephemeral cache (all keys can be evicted if needed)?
- A) volatile-lru
- B) noeviction
- C) allkeys-lru ✓
- D) volatile-ttl

**Q31.** A high `ReplicationLag` on ElastiCache Read Replica typically indicates:
- A) Too many client connections to the replica
- B) A slow or expensive operation blocking the primary's replication stream ✓
- C) Network packet loss between AZs
- D) The replica node type is too large

**Q32.** Redis pub/sub vs SQS: SQS is preferred when:
- A) Messages are fire-and-forget with sub-millisecond latency requirement
- B) Durability, acknowledgement, dead-letter queue, and retry are required ✓
- C) The subscriber count exceeds 100
- D) You need to broadcast to multiple subscribers simultaneously

**Q33.** What is a cache stampede (thundering herd)?
- A) A single large key causing Redis to slow all operations
- B) Many simultaneous cache misses causing all requests to hit the DB at once ✓
- C) The eviction policy deleting too many keys
- D) Cluster nodes failing simultaneously under load

**Q34.** Which ElastiCache for Redis feature allows write scaling across multiple shards?
- A) Multi-AZ replication
- B) Read replicas per shard
- C) Cluster mode (enabled) ✓
- D) ElastiCache Serverless

**Q35.** The `SCAN` command is preferred over `KEYS *` in production because:
- A) SCAN is O(1) while KEYS is O(N)
- B) SCAN iterates incrementally without blocking the event loop ✓
- C) SCAN returns sorted results
- D) SCAN only works in cluster mode

**Q36.** What does `lazyfree-lazy-expire yes` do in Redis?
- A) Delays setting a TTL until the next write
- B) Expires keys in the background to avoid blocking ✓
- C) Lazily replaces evicted keys with null values
- D) Disables automatic expiry globally

**Q37.** At what `used_memory` percentage should you plan to scale an ElastiCache cluster?
- A) 50%
- B) 70% ✓
- C) 90%
- D) 100%

**Q38.** Which Redis data structure is BEST for implementing a sliding window rate limiter?
- A) String
- B) List
- C) Sorted set ✓
- D) Hash

**Q39.** When rotating ElastiCache Redis AUTH tokens, the most important step for zero downtime is:
- A) Delete the old token immediately to force all clients to reconnect
- B) Enable the new token while keeping the old one active until all clients are updated ✓
- C) Restart the Redis cluster with the new token
- D) Use a Lambda function to rotate all client credentials simultaneously

**Q40.** ElastiCache for Redis can persist data using:
- A) EBS snapshots and S3 exports
- B) RDB snapshots and/or AOF (Append Only File) ✓
- C) DynamoDB export and S3 standard storage
- D) Redis DUMP command to S3

**Section B answer key:** 21-C, 22-C, 23-D, 24-B, 25-C, 26-B, 27-A, 28-B, 29-C, 30-C, 31-B, 32-B, 33-B, 34-C, 35-B, 36-B, 37-B, 38-C, 39-B, 40-B

---

## Section C — SQS & SNS (Q41–60)

**Q41.** The maximum visibility timeout for an SQS message is:
- A) 12 hours ✓
- B) 24 hours
- C) 7 days
- D) 1 hour

**Q42.** SQS long polling sets `WaitTimeSeconds` to a value up to:
- A) 5 seconds
- B) 10 seconds
- C) 20 seconds ✓
- D) 60 seconds

**Q43.** An SQS FIFO queue guarantees:
- A) At-most-once delivery
- B) Exactly-once processing within a 5-minute deduplication window ✓
- C) At-least-once delivery like Standard queues
- D) Sub-second latency for all messages

**Q44.** The `MessageGroupId` attribute in SQS FIFO queues controls:
- A) Message priority within the queue
- B) Message deduplication across queues
- C) The ordering scope — messages with the same group ID are delivered in order ✓
- D) The maximum number of inflight messages

**Q45.** A Dead Letter Queue receives a message after it fails to process:
- A) Once
- B) `maxReceiveCount` times ✓
- C) Three times by default
- D) When the visibility timeout expires the first time

**Q46.** The SNS fan-out pattern means:
- A) One SQS queue distributes messages to multiple Lambda functions
- B) One SNS topic delivers to multiple SQS queues (one per subscriber service) ✓
- C) One Lambda function publishes to multiple SNS topics
- D) SNS delivers directly to EC2 instances

**Q47.** SNS message filtering policies are applied:
- A) At the publisher before sending to the topic
- B) At each subscription, so only matching messages are delivered ✓
- C) At the SNS topic level, blocking mismatched messages
- D) By the consumer after receiving the message

**Q48.** SQS `DelaySeconds` makes a newly sent message:
- A) Visible after the specified delay ✓
- B) Invisible after being received (same as visibility timeout)
- C) Deleted after the specified delay if not consumed
- D) Moved to DLQ after the specified delay

**Q49.** What is the maximum SQS message size?
- A) 256 KB ✓
- B) 1 MB
- C) 10 MB
- D) 64 KB

**Q50.** The SQS Extended Client Library solves which problem?
- A) SQS FIFO ordering across multiple queues
- B) Payloads larger than 256 KB by offloading to S3 ✓
- C) Cross-account SQS access
- D) Message deduplication in Standard queues

**Q51.** When Lambda processes SQS messages and returns `batchItemFailures`:
- A) The entire batch is re-processed
- B) Only the failed messages in the batch return to the queue ✓
- C) All messages are deleted
- D) Failed messages go directly to DLQ

**Q52.** Which SQS metric indicates consumer lag (how far behind consumers are)?
- A) NumberOfMessagesSent
- B) ApproximateNumberOfMessagesNotVisible
- C) ApproximateAgeOfOldestMessage ✓
- D) NumberOfMessagesDeleted

**Q53.** To cross-account publish from Account A's Lambda to Account B's SQS queue, you need:
- A) A VPC peering between accounts A and B
- B) Account B's SQS queue resource policy to allow Account A to `sqs:SendMessage` ✓
- C) An IAM role in Account A with `sqs:SendMessage` on any resource
- D) A Transit Gateway connecting both accounts

**Q54.** SNS FIFO topic subscribers must be:
- A) Lambda functions only
- B) SQS FIFO queues only ✓
- C) Any SQS queue type
- D) HTTP endpoints with deduplication support

**Q55.** How do you redrive messages from a DLQ back to the source queue in SQS?
- A) There is no native SQS DLQ redrive feature
- B) `aws sqs start-message-move-task --source-arn DLQ-ARN --destination-arn SOURCE-QUEUE-ARN` ✓
- C) Subscribe the source queue to the DLQ as an SNS topic
- D) Use `ChangeMessageVisibility` on the DLQ

**Q56.** At-least-once delivery in Standard SQS means your consumer must be:
- A) Fast (< 30 seconds per message)
- B) Idempotent ✓
- C) Running on EC2 (not Lambda)
- D) Using FIFO queues

**Q57.** EventBridge vs SNS: EventBridge is preferred when you need:
- A) Simple pub/sub with no schema requirement
- B) Lowest possible delivery latency
- C) Cross-account routing, event replay, schema registry, and scheduled rules ✓
- D) Native integration with SES for email delivery

**Q58.** SQS FIFO queue throughput limit without high-throughput mode is:
- A) 100 msg/s per queue
- B) 300 msg/s per queue ✓
- C) 3,000 msg/s per queue
- D) Unlimited like Standard queues

**Q59.** An SNS subscription filter policy attribute supports which operators?
- A) Exact match, prefix, numeric ranges ✓
- B) Regex and wildcard patterns
- C) XPath expressions
- D) SQL WHERE clauses

**Q60.** When scaling Lambda consumers for SQS, what determines the number of concurrent Lambda executions?
- A) The number of SQS queue partitions
- B) The number of inflight messages divided by batch size ✓
- C) The `ReservedConcurrency` setting on the queue
- D) The SQS `MaximumConcurrency` setting on the event source mapping

**Section C answer key:** 41-A, 42-C, 43-B, 44-C, 45-B, 46-B, 47-B, 48-A, 49-A, 50-B, 51-B, 52-C, 53-B, 54-B, 55-B, 56-B, 57-C, 58-B, 59-A, 60-B

---

## Section D — Terraform (Q61–80)

**Q61.** Where is Terraform state stored when using remote state on AWS?
- A) DynamoDB table only
- B) S3 bucket for state file; DynamoDB for state locking ✓
- C) RDS database managed by Terraform Cloud
- D) EFS for shared team access

**Q62.** A `moved` block in Terraform is used to:
- A) Physically move a resource to another AWS region
- B) Move a resource between Terraform roots without destroying it
- C) Rename a resource in state without destroying and recreating ✓
- D) Transfer resources between workspaces

**Q63.** `lifecycle { prevent_destroy = true }` will:
- A) Block all updates to the resource
- B) Prevent `terraform destroy` from removing the resource ✓
- C) Prevent the resource from being replaced
- D) Force a manual approval before any change

**Q64.** `for_each` is preferred over `count` for creating similar resources because:
- A) `for_each` is faster to apply
- B) `for_each` supports more resource types
- C) `for_each` uses stable string keys, avoiding destroy/recreate on reordering ✓
- D) `count` doesn't support maps as input

**Q65.** OIDC federation in Terraform CI means:
- A) GitHub Actions uses an IAM access key stored as a secret
- B) GitHub Actions assumes an IAM role via OIDC with no stored AWS credentials ✓
- C) Terraform Cloud authenticates using a root account token
- D) A shared IAM user provides cross-team access

**Q66.** The Terraform `.terraform.lock.hcl` file:
- A) Locks the AWS region for all resources
- B) Locks provider versions to ensure reproducible builds ✓
- C) Prevents other team members from running `terraform apply` simultaneously
- D) Stores the DynamoDB lock token

**Q67.** The `-target` flag in `terraform apply` is dangerous because:
- A) It bypasses provider authentication
- B) It applies changes without generating a plan
- C) It can leave state inconsistent by skipping dependency evaluation ✓
- D) It destroys non-targeted resources

**Q68.** `terraform import` does which of the following?
- A) Exports an existing resource to a new Terraform config file
- B) Brings an existing manually-created resource under Terraform state control ✓
- C) Imports a remote Terraform module into the local directory
- D) Syncs the remote state with the local .terraform directory

**Q69.** A `data` source in Terraform:
- A) Creates a new AWS resource and manages its lifecycle
- B) Reads existing AWS infrastructure without managing it ✓
- C) Outputs a computed value from a resource
- D) Defines input variables for a module

**Q70.** Atlantis and Spacelift are used with Terraform to:
- A) Generate CloudFormation templates from Terraform configs
- B) Provide CI/CD automation for Terraform plan/apply via pull requests ✓
- C) Manage Terraform provider version upgrades automatically
- D) Create Terraform modules from existing AWS infrastructure

**Q71.** Terragrunt's primary benefit over raw Terraform is:
- A) Faster plan and apply execution
- B) Native support for AWS CloudFormation
- C) DRY configuration with backend and dependency management for multi-account setups ✓
- D) Built-in state encryption at rest

**Q72.** Which Terraform version introduced `terraform_data` as a replacement for `null_resource`?
- A) 1.2
- B) 1.4 ✓
- C) 1.6
- D) 2.0

**Q73.** How do you pass outputs from one Terraform root to another?
- A) Use a module inside the second root that reads the first root's state
- B) Use `terraform_remote_state` data source pointing to the first root's backend ✓
- C) Hardcode output values as variables in the second root
- D) Use `terraform output -json` in a CI script

**Q74.** `ignore_changes = [tags]` in a lifecycle block will:
- A) Delete the resource if tags change
- B) Apply tag changes automatically without a plan
- C) Prevent Terraform from showing or applying changes to the `tags` attribute ✓
- D) Ignore all attributes except tags during plan

**Q75.** Terraform workspaces are BEST suited for:
- A) Managing a single large application across hundreds of microservices
- B) Identical environment replicas (dev/staging/prod) with minimal config differences ✓
- C) Managing cross-account infrastructure with different providers
- D) Isolating provider version upgrades

**Q76.** `create_before_destroy = true` is useful when:
- A) You want to avoid momentary downtime caused by replacing a resource ✓
- B) You want to test a resource before it receives traffic
- C) You want to create a backup before deleting the old resource
- D) The resource has a `prevent_destroy` constraint

**Q77.** The `~> 5.60` version constraint in a Terraform provider block means:
- A) Any version ≥ 5.60 and < 6.0.0 ✓
- B) Any version ≥ 5.0.0 and < 6.0.0
- C) Exactly version 5.60
- D) Version 5.60 or higher, no upper bound

**Q78.** Terratest is used for:
- A) Linting Terraform configs for best practices
- B) Scanning Terraform configs for security issues
- C) Go-based integration tests that apply Terraform, assert results, and destroy ✓
- D) Generating Terraform configs from existing AWS infrastructure

**Q79.** A "state lock stuck" issue in Terraform + DynamoDB is resolved by:
- A) Deleting the DynamoDB table and re-initializing
- B) Running `terraform force-unlock LOCK_ID` after verifying no apply is running ✓
- C) Manually deleting the state file from S3
- D) Running `terraform refresh` to clear the lock

**Q80.** What does `sensitive = true` on a Terraform variable do?
- A) Encrypts the variable value in the state file
- B) Prevents the variable value from appearing in plan/apply output ✓
- C) Requires the variable to be passed via AWS Secrets Manager
- D) Marks the resource as requiring manual approval before apply

**Section D answer key:** 61-B, 62-C, 63-B, 64-C, 65-B, 66-B, 67-C, 68-B, 69-B, 70-B, 71-C, 72-B, 73-B, 74-C, 75-B, 76-A, 77-A, 78-C, 79-B, 80-B

---

## Section E — CloudFormation (Q81–95)

**Q81.** A CloudFormation change set shows an action of `Replacement: True` for an RDS instance. This means:
- A) CloudFormation will update the instance in place
- B) CloudFormation will delete the old instance and create a new one ✓
- C) CloudFormation will take a snapshot before modifying
- D) The change is conditional on a parameter

**Q82.** Which CloudFormation function returns the ARN of a resource created in the same template?
- A) `!Ref`
- B) `!GetAtt` ✓
- C) `!Sub`
- D) `!ImportValue`

**Q83.** `!ImportValue` is used to:
- A) Import a parameter from SSM Parameter Store
- B) Import an AMI ID from the EC2 marketplace
- C) Consume an exported output value from another CloudFormation stack ✓
- D) Import a resource from AWS Config

**Q84.** Which permission model for CloudFormation StackSets allows automatic deployment to new accounts added to an OU?
- A) SELF_MANAGED
- B) SERVICE_MANAGED ✓
- C) ORGANIZATION_MANAGED
- D) DELEGATED_ADMIN

**Q85.** What does `NoEcho: true` on a CloudFormation parameter do?
- A) Makes the parameter optional
- B) Prevents the parameter value from appearing in the console or API responses ✓
- C) Encrypts the value with KMS before storing
- D) Sets the parameter to a default empty string

**Q86.** CloudFormation drift detection identifies:
- A) Resources whose configuration was changed outside of CloudFormation ✓
- B) Outdated AMIs in Auto Scaling Groups
- C) Resources that failed to create and require manual rollback
- D) Circular dependencies in the template

**Q87.** A CloudFormation stack stuck in `UPDATE_ROLLBACK_FAILED` can be recovered using:
- A) `aws cloudformation delete-stack --force`
- B) `aws cloudformation continue-update-rollback --skip-resources <LogicalId>` ✓
- C) `aws cloudformation cancel-update-stack`
- D) Deleting the failed resource in the console and re-running the update

**Q88.** Which dynamic reference syntax reads a secret from Secrets Manager directly in a CloudFormation template?
- A) `{{ssm:/my/param}}`
- B) `{{resolve:ssm-secure:/my/param}}`
- C) `{{resolve:secretsmanager:arn:aws:secretsmanager:us-east-1:123:secret:my-secret}}` ✓
- D) `{{secrets-manager:my-secret:key}}`

**Q89.** AWS CDK differs from CloudFormation YAML in that CDK:
- A) Deploys resources without creating a CloudFormation template
- B) Uses proprietary deployment APIs that bypass CloudFormation
- C) Synthesizes CloudFormation templates from code (TypeScript/Python/etc.) ✓
- D) Only works with AWS Lambda and S3

**Q90.** A `CreationPolicy` with a `ResourceSignal` is used to:
- A) Trigger a Lambda when a resource is created
- B) Wait for an EC2 instance to signal it is healthy before marking creation complete ✓
- C) Set a timeout for resource creation
- D) Require manual approval before resource creation

**Q91.** Cross-stack CloudFormation exports have a key limitation:
- A) You can only export 10 values per stack
- B) Exported values cannot be updated while imported by another stack ✓
- C) Exports expire after 90 days
- D) Cross-region imports require StackSets

**Q92.** What is an AWS CloudFormation macro?
- A) A reusable parameter block shared across stacks
- B) A template transform processed by a Lambda function before CloudFormation executes ✓
- C) A YAML alias that shortens repeated template sections
- D) A CloudFormation condition that spans multiple resources

**Q93.** `UpdateReplacePolicy: Snapshot` on an RDS resource means:
- A) CloudFormation takes a snapshot before any update
- B) CloudFormation takes a snapshot before replacing (deleting) the old resource ✓
- C) CloudFormation prevents the resource from being replaced
- D) A snapshot is taken daily during updates

**Q94.** In a CloudFormation nested stack, if the child stack creation fails:
- A) Only the child stack rolls back; the parent continues
- B) The parent stack pauses and waits for manual intervention
- C) The parent stack rolls back, which also rolls back all nested stacks ✓
- D) The child stack is skipped and the parent completes with warnings

**Q95.** The AWS SAM (Serverless Application Model) transform in CloudFormation:
- A) Creates EC2 instances and RDS databases from a simplified YAML
- B) Expands `AWS::Serverless::Function` and related resources into full CloudFormation ✓
- C) Deploys ECS services without needing a task definition
- D) Manages CloudFormation stacks across multiple accounts

**Section E answer key:** 81-B, 82-B, 83-C, 84-B, 85-B, 86-A, 87-B, 88-C, 89-C, 90-B, 91-B, 92-B, 93-B, 94-C, 95-B

---

## Section F — WAF & Shield (Q96–110)

**Q96.** A WAF Web ACL attached to a CloudFront distribution must be created in which region?
- A) The region where CloudFront's origin is located
- B) us-east-1 (global scope) ✓
- C) Any region (WAF is global)
- D) The region with the highest traffic

**Q97.** WAF rules are evaluated in order of:
- A) Rule type (managed vs custom)
- B) Priority number (lower number = evaluated first) ✓
- C) Creation date (oldest first)
- D) Rule size (smallest first)

**Q98.** Setting a WAF rule to `Count` mode instead of `Block` mode does what?
- A) Permanently allows traffic matching the rule
- B) Counts matching requests but allows them, enabling safe testing before blocking ✓
- C) Blocks requests and logs them to CloudWatch
- D) Enables rate limiting for matched requests

**Q99.** A WAF rate-based rule counts requests over a fixed window of:
- A) 1 minute
- B) 5 minutes ✓
- C) 10 minutes
- D) 1 hour

**Q100.** Shield Standard is:
- A) A paid service at $3,000/month
- B) Automatically enabled for all AWS customers at no extra cost, protecting against L3/L4 DDoS ✓
- C) Available only with Business or Enterprise support plans
- D) Enabled by default only for CloudFront distributions

**Q101.** Shield Advanced, compared to Standard, additionally provides:
- A) L3/L4 protection (Standard already covers this), which is the same level
- B) L7 protection (via WAF), Shield Response Team access, and cost protection ✓
- C) Only a management console dashboard for attack visibility
- D) Automatic WAF rule creation without configuration

**Q102.** WAF Bot Control is a:
- A) Custom rule you write to block bots
- B) AWS managed rule group that classifies and controls bot traffic ✓
- C) Shield Advanced feature for bot-specific DDoS mitigation
- D) Lambda@Edge function for challenge/response

**Q103.** To implement per-IP rate limiting in WAF where each IP is limited to 1,000 requests per 5 minutes:
- A) Use a WAF IP set rule
- B) Use a WAF rate-based rule with aggregate key type = IP ✓
- C) Use a WAF managed rule group
- D) Configure ALB connection throttling

**Q104.** A "scope-down statement" in a WAF rate-based rule:
- A) Reduces the WAF Web ACL's scope to fewer AWS resources
- B) Narrows the rate-based rule to apply only to requests matching specific criteria ✓
- C) Limits the number of rules in a Web ACL
- D) Restricts WAF to specific HTTP methods

**Q105.** WAF logging to Kinesis Data Firehose enables:
- A) Real-time blocking of attacker IPs
- B) Storing WAF logs in S3 for Athena analysis ✓
- C) Automatic CloudWatch alarm creation from WAF logs
- D) SNS notifications for every blocked request

**Q106.** When WAF is attached to CloudFront, WAF rules execute:
- A) At the origin server
- B) At the ALB in front of the origin
- C) At the CloudFront PoP ✓
- D) At AWS Shield infrastructure

**Q107.** The WAF `CAPTCHA` action:
- A) Permanently blocks the client
- B) Challenges the client with a puzzle; successful completions receive a time-limited token ✓
- C) Redirects the client to a captcha.amazonaws.com page and blocks indefinitely
- D) Rate limits the client to 1 request per minute

**Q108.** Which WAF managed rule group specifically targets SQL injection and XSS?
- A) AWSManagedRulesCommonRuleSet
- B) AWSManagedRulesAmazonIpReputationList
- C) AWSManagedRulesSQLiRuleSet ✓
- D) AWSManagedRulesBotControlRuleSet

**Q109.** To reduce false positives in WAF for a JSON API:
- A) Set all managed rules to Count and never switch to Block
- B) Apply managed rules with a scope-down statement excluding API paths from specific rules ✓
- C) Create a separate WAF ACL for each API endpoint
- D) Disable managed rule groups and write all rules manually

**Q110.** Shield Advanced SRT (Shield Response Team) provides:
- A) Automated Lambda-based DDoS response
- B) 24/7 expert assistance for L3-L7 attacks, including custom WAF rule writing ✓
- C) Replacement of all AWS network infrastructure during an attack
- D) Guaranteed 100% uptime SLA during any DDoS event

**Section F answer key:** 96-B, 97-B, 98-B, 99-B, 100-B, 101-B, 102-B, 103-B, 104-B, 105-B, 106-C, 107-B, 108-C, 109-B, 110-B

---

## Section G — Organizations & Multi-Account (Q111–125)

**Q111.** A Service Control Policy (SCP) attached to an OU:
- A) Grants permissions to all IAM users in the OU
- B) Restricts the maximum permissions available to principals in OU accounts ✓
- C) Replaces IAM policies for resources in the OU
- D) Enables cross-account access for the OU member accounts

**Q112.** Which account should NEVER have any workloads or IAM users with programmatic access?
- A) Security Tooling account
- B) Network Hub account
- C) Management (root) account ✓
- D) Shared Services account

**Q113.** AWS Control Tower is used to:
- A) Monitor AWS resource costs across the organisation
- B) Set up a multi-account landing zone with OU structure, SCPs, and guardrails ✓
- C) Manage CloudFormation StackSets manually
- D) Consolidate VPCs across accounts into a Transit Gateway

**Q114.** AWS RAM (Resource Access Manager) is used to:
- A) Create IAM roles automatically in member accounts
- B) Share AWS resources (VPC subnets, TGW, etc.) across accounts without copying ✓
- C) Replicate S3 buckets cross-account
- D) Centralise billing for Reserved Instances

**Q115.** An SCP with `"Effect": "Deny"` and `"Action": "organizations:LeaveOrganization"` will:
- A) Prevent the management account from removing member accounts
- B) Prevent any account in the scope from leaving the organisation ✓
- C) Block cross-account IAM role assumption
- D) Disable AWS Organisations API calls for member accounts

**Q116.** A delegated administrator for GuardDuty is set up to:
- A) Manage GuardDuty from the root account's console
- B) Allow a member account (e.g., Security Tooling) to manage GuardDuty for the org ✓
- C) Enable GuardDuty in new accounts automatically via an IAM policy
- D) Aggregate GuardDuty findings to a single S3 bucket

**Q117.** Cross-account role chaining allows a maximum of how many hops in a session chain?
- A) 3
- B) 5 ✓
- C) 10
- D) Unlimited

**Q118.** The "Log Archive" account in an AWS landing zone is specifically responsible for:
- A) Running log analysis tools (CloudWatch, Athena, OpenSearch)
- B) Storing immutable, centralised logs (CloudTrail, Config) from all accounts ✓
- C) Providing network routing between spoke VPCs
- D) Hosting the AWS Config aggregator

**Q119.** Account Factory for Terraform (AFT) automates:
- A) CloudFormation stack deployment across accounts
- B) Network topology creation using Transit Gateway
- C) Vending new AWS accounts with baseline configuration via Terraform and Git ✓
- D) IAM role creation and permission assignment in new accounts

**Q120.** Consolidated billing in AWS Organisations benefits member accounts because:
- A) Each account gets its own AWS Credits allocation
- B) Reserved Instances and Savings Plans purchased in any account apply org-wide ✓
- C) All accounts share the same service quotas
- D) SCP restrictions don't apply to accounts with billing admin access

**Q121.** To prevent S3 buckets from being made public across all organisation accounts, the BEST approach is:
- A) Enable ACL blocking in the management account
- B) SCP denying `s3:PutBucketPublicAccessBlock` removal + S3 account-level block via StackSet ✓
- C) Create a Config rule in each account
- D) Enable GuardDuty S3 protection in the management account

**Q122.** In an organisation with many accounts, how are VPCs across accounts connected with centralised routing?
- A) VPC Peering between each pair of accounts
- B) AWS PrivateLink endpoints in each account
- C) Transit Gateway (TGW) with each account's VPC attached as a spoke ✓
- D) ClassicLink for legacy account connectivity

**Q123.** An IAM Identity Center (SSO) in an organisation allows users to:
- A) Create IAM users in every member account simultaneously
- B) Access multiple accounts with a single identity via permission sets ✓
- C) Use root account credentials to access member accounts
- D) Share IAM roles between member accounts

**Q124.** A sandbox OU is different from a workload OU because sandbox accounts:
- A) Have stricter SCPs than production accounts
- B) Are used for developer experimentation with auto-cleanup or budget limits ✓
- C) Cannot create EC2 instances or RDS databases
- D) Have no billing alerts enabled

**Q125.** The effective permission for an IAM action in an OU member account is:
- A) The union of IAM policy + SCP
- B) The IAM policy alone (SCP is advisory)
- C) The intersection of IAM policy AND SCP (both must allow; any deny wins) ✓
- D) The SCP alone (IAM is ignored in Organizations)

**Section G answer key:** 111-B, 112-C, 113-B, 114-B, 115-B, 116-B, 117-B, 118-B, 119-C, 120-B, 121-B, 122-C, 123-B, 124-B, 125-C

---

## Section H-I — Enterprise Architecture & Multi-Region DR (Q126–155)

**Q126.** Hub-and-spoke topology with Transit Gateway is preferred over VPC peering because:
- A) VPC peering doesn't support IPv6
- B) VPC peering costs more per GB transferred
- C) TGW requires only N connections vs N(N-1)/2 for full-mesh peering ✓
- D) TGW supports cross-region connectivity natively (peering doesn't)

**Q127.** Global Accelerator differs from CloudFront in that Global Accelerator:
- A) Provides content caching at edge locations
- B) Only supports HTTP and HTTPS traffic
- C) Uses anycast static IPs and routes TCP/UDP traffic over the AWS backbone ✓
- D) Requires Lambda@Edge for custom routing logic

**Q128.** Direct Connect with a VPN backup provides:
- A) Higher bandwidth than Direct Connect alone
- B) Automatic failover from DX to VPN if the DX circuit fails ✓
- C) Lower latency than Direct Connect alone
- D) Encryption on the Direct Connect circuit

**Q129.** The strangler-fig migration pattern means:
- A) Rewriting the entire monolith before deploying any microservices
- B) Extracting functionality from the monolith incrementally while keeping it running ✓
- C) Running two identical systems in parallel until the new one is stable
- D) Migrating data first, then application code

**Q130.** CQRS (Command Query Responsibility Segregation) on AWS is typically implemented with:
- A) RDS Multi-AZ for both reads and writes
- B) Separate write path (RDS/SQS) and read path (ElastiCache/DynamoDB/Redshift) ✓
- C) Read replicas in the same RDS cluster for all queries
- D) DynamoDB with a GSI for write operations

**Q131.** Aurora Global Database's replication lag is typically:
- A) < 5 seconds
- B) < 1 second ✓
- C) 15 minutes (RTC-level)
- D) Near-zero with multi-master enabled

**Q132.** Route 53 failover routing switches traffic when:
- A) The primary region's latency exceeds a threshold
- B) The primary endpoint's Route 53 health check fails ✓
- C) An AWS Personal Health Dashboard event is published
- D) CloudWatch composite alarm fires

**Q133.** S3 Replication Time Control (RTC) guarantees:
- A) All objects replicate in under 1 second
- B) 99.99% of objects replicate within 15 minutes ✓
- C) Objects replicate in under 5 minutes on average
- D) Objects replicate within 1 hour, guaranteed

**Q134.** DynamoDB Global Tables handle write conflicts using:
- A) Optimistic locking with version numbers
- B) Last-writer-wins based on the item's timestamp ✓
- C) First-writer-wins (second write is rejected)
- D) A conflict resolution Lambda function

**Q135.** KMS keys are regional. What does this mean for cross-region RDS snapshot copies?
- A) KMS keys are automatically copied with the snapshot
- B) The snapshot copy is re-encrypted with a CMK in the target region ✓
- C) Cross-region snapshot copies are not encrypted
- D) The same CMK ARN works in all regions

**Q136.** Pilot Light DR on AWS means:
- A) A full production environment running in the DR region at all times
- B) Core infrastructure (DB replica, minimal compute) running; scale up on disaster ✓
- C) All data backed up to S3; restore from scratch on disaster
- D) Traffic running in both regions simultaneously

**Q137.** AWS FIS (Fault Injection Simulator) is used to:
- A) Monitor application performance during peak load
- B) Simulate infrastructure failures to test application resilience ✓
- C) Generate synthetic traffic for load testing
- D) Detect and quarantine compromised EC2 instances

**Q138.** RDS blue/green deployments are used for:
- A) Routing 50% of production traffic to a standby for load balancing
- B) Zero-downtime major version upgrades with a parallel secondary (green) instance ✓
- C) Blue environment = read replicas; green = primary
- D) Cross-region replication with automatic failover

**Q139.** The order of operations when failing over to a DR region should be:
- A) Flip DNS → promote DB → update app config
- B) Promote DB → update app config/secrets → flip DNS ✓
- C) Update app config → flip DNS → promote DB
- D) All simultaneously via a pre-written runbook

**Q140.** AWS Resilience Hub provides:
- A) Managed DR infrastructure provisioning
- B) Analysis of your application against RTO/RPO targets with gap identification ✓
- C) Automatic failover to a secondary region
- D) Load testing at scale to validate resilience

**Q141.** An Active-Active multi-region architecture for a database layer requires which Aurora feature?
- A) Aurora Read Replicas
- B) Aurora Global Database (readable secondary)
- C) Aurora Multi-Master or Aurora Limitless (for true active-active writes) ✓
- D) Aurora Serverless v2

**Q142.** Secrets Manager cross-region replication:
- A) Requires a custom Lambda function to copy secrets
- B) Is natively supported: primary secret replicates to replica regions automatically ✓
- C) Replicates secrets only to us-east-1 as the global hub
- D) Is available only for database credential secrets

**Q143.** For a 10-minute RTO, which DR strategy is appropriate?
- A) Backup and Restore
- B) Pilot Light
- C) Warm Standby ✓
- D) Cold Standby

**Q144.** S3 Cross-Region Replication requires which S3 feature on the source bucket?
- A) Versioning enabled ✓
- B) Static website hosting enabled
- C) S3 Transfer Acceleration enabled
- D) Public access allowed

**Q145.** The four DR strategies in order from lowest to highest cost and lowest to highest availability are:
- A) Pilot Light → Warm Standby → Backup & Restore → Active-Active
- B) Backup & Restore → Pilot Light → Warm Standby → Active-Active ✓
- C) Warm Standby → Pilot Light → Active-Active → Backup & Restore
- D) Active-Active → Warm Standby → Backup & Restore → Pilot Light

**Q146.** When DynamoDB Global Tables replicate data, writes in Region A are:
- A) Queued until acknowledged by Region B (synchronous)
- B) Applied asynchronously to Region B ✓
- C) Rejected if Region B is unreachable
- D) Buffered in SQS before replication

**Q147.** Route 53 latency routing sends users to:
- A) The region with the lowest cost
- B) The region with the most available capacity
- C) The region with the lowest measured network latency from the user ✓
- D) The region closest geographically

**Q148.** A DR drill methodology should include:
- A) Testing only in staging, never in production to avoid risk
- B) Tabletop exercise + recovery runbook + periodic live failover test ✓
- C) Automated failover only, no manual drills
- D) Annual testing is sufficient for most workloads

**Q149.** Cross-region Secrets Manager replica secrets should be used when:
- A) You want to rotate secrets more frequently
- B) DR or multi-region apps need to read secrets with low latency in each region ✓
- C) Your primary region's secret has been compromised
- D) You need different credentials per region

**Q150.** Aurora Global Database unplanned failover (promoted secondary) achieves RTO of approximately:
- A) < 5 seconds
- B) < 1 minute ✓
- C) < 5 minutes
- D) < 30 minutes

**Q151.** Backup and Restore DR has which RPO/RTO profile?
- A) RPO: hours/days, RTO: hours/days ✓
- B) RPO: minutes, RTO: minutes
- C) RPO: seconds, RTO: seconds
- D) RPO: zero, RTO: zero

**Q152.** For a global SaaS, Route 53 weighted routing shifts all traffic from Region A to Region B by:
- A) Setting Region A weight to 0 and Region B weight to 100 ✓
- B) Deleting the Region A health check
- C) Setting the TTL to 0 seconds
- D) Updating both A-records to point to Region B

**Q153.** DynamoDB Point-in-Time Recovery (PITR) retains continuous backups for:
- A) 24 hours
- B) 7 days
- C) 35 days ✓
- D) 90 days

**Q154.** When testing DR, which approach is safest for validating database failover?
- A) Perform a manual failover of production during a maintenance window
- B) Restore a snapshot to the DR region and run integration tests in isolation ✓
- C) Stop replication to the replica to simulate lag
- D) Delete the primary DB to test automatic DNS failover

**Q155.** The Recovery Time Objective (RTO) is defined as:
- A) How much data can be lost (measured in time)
- B) How long the business can tolerate the system being down ✓
- C) The time between automated backups
- D) The time required to detect a failure

**Section H-I answer key:** 126-C, 127-C, 128-B, 129-B, 130-B, 131-B, 132-B, 133-B, 134-B, 135-B, 136-B, 137-B, 138-B, 139-B, 140-B, 141-C, 142-B, 143-C, 144-A, 145-B, 146-B, 147-C, 148-B, 149-B, 150-B, 151-A, 152-A, 153-C, 154-B, 155-B

---

## Section J-K — Scalability & Security (Q156–180)

**Q156.** RDS Proxy is MOST critical when:
- A) You need to add read replicas to a Single-AZ deployment
- B) ECS Fargate tasks each open connection pools, exhausting RDS max_connections ✓
- C) You want to route read queries to a read replica automatically
- D) Your database query latency exceeds 100ms

**Q157.** ECS Fargate Spot is appropriate for which workload type?
- A) Synchronous API serving user requests with strict p99 SLA
- B) Asynchronous SQS consumers and batch jobs that can tolerate interruption ✓
- C) Database primary instances requiring high availability
- D) Container images that take > 2 minutes to start

**Q158.** Karpenter (for EKS) compared to Cluster Autoscaler:
- A) Is slower but more predictable in node selection
- B) Requires more configuration but supports more instance types
- C) Provisions the right-sized node for each pending pod in ~30 seconds ✓
- D) Only works with On-Demand EC2 instances

**Q159.** Target tracking auto-scaling for ASG on CPU at 60% means:
- A) Scale in when CPU drops below 60%, scale out when it exceeds 60% ✓
- B) Always keep exactly 60% of max capacity running
- C) Alert when CPU exceeds 60% but don't scale
- D) Scale out by 60% of current capacity on any CPU alarm

**Q160.** DynamoDB single-table design with a composite key (PK + SK) is used to:
- A) Enforce strict relational constraints
- B) Enable multiple entity types and access patterns in one table, reducing hot partitions ✓
- C) Simplify table migrations
- D) Allow multi-table transactions

**Q161.** What is the primary reason to enable DynamoDB auto-scaling vs provisioned throughput?
- A) Auto-scaling is cheaper in all scenarios
- B) Auto-scaling adjusts RCU/WCU dynamically for unpredictable traffic without manual updates ✓
- C) Auto-scaling enables point-in-time recovery
- D) Provisioned throughput has lower latency

**Q162.** An ElastiCache Read Replica in a non-cluster-mode setup scales:
- A) Write throughput
- B) Total memory capacity
- C) Read throughput ✓
- D) Connection throughput

**Q163.** SQS with ECS consumer auto-scaling should scale based on:
- A) SQS `NumberOfMessagesSent` metric
- B) SQS `ApproximateNumberOfMessagesVisible` / running task count ✓
- C) CPU utilisation of ECS tasks
- D) SQS `ApproximateAgeOfOldestMessage` only

**Q164.** Predictive scaling for ASG:
- A) Reacts to real-time CPU spikes faster than target tracking
- B) Forecasts traffic based on historical patterns and pre-scales ahead of predicted peaks ✓
- C) Is the same as scheduled scaling but with random jitter
- D) Requires AWS Forecast service integration

**Q165.** Aurora Serverless v2 differs from Aurora Serverless v1 in that v2:
- A) Can scale to zero when idle (like v1)
- B) Scales in fine-grained ACU increments without cold start delays ✓
- C) Is only available for PostgreSQL
- D) Cannot use Multi-AZ

**Q166.** Zero Trust network architecture on AWS means:
- A) Trust all traffic from within the VPC; only restrict internet traffic
- B) Never trust network location; verify identity and intent for every request ✓
- C) Block all inbound traffic by default; allow only IAM-signed requests
- D) Remove all security groups and use network ACLs only

**Q167.** GuardDuty detects threats by analyzing:
- A) CloudWatch logs and custom application metrics
- B) CloudTrail events, VPC flow logs, and DNS logs ✓
- C) S3 object content and DynamoDB item data
- D) Security group changes and IAM policy modifications

**Q168.** An IAM permission boundary:
- A) Sets the maximum permissions a role can grant to another role
- B) Adds an additional layer of permissions beyond what the policy allows
- C) Sets the maximum permissions any policy can grant to the principal ✓
- D) Restricts which AWS services can be accessed from a VPC

**Q169.** KMS key rotation (automatic) for a CMK rotates:
- A) The key material and the key ID simultaneously
- B) The key material only; the key ID and ARN remain the same ✓
- C) The key ID only; old key material is permanently deleted
- D) The key every 30 days by default

**Q170.** AWS Macie is specifically used to:
- A) Detect network-level DDoS attacks in real time
- B) Discover and protect sensitive data (PII, PHI, PCI data) in S3 ✓
- C) Analyse IAM policy configurations for overly permissive access
- D) Monitor EC2 instances for malware and rootkits

**Q171.** A VPC endpoint for S3 is of type:
- A) Interface (PrivateLink)
- B) Gateway ✓
- C) Transit
- D) Service

**Q172.** AWS Security Hub aggregates findings from:
- A) CloudTrail only
- B) GuardDuty only
- C) GuardDuty, Macie, Inspector, Config, IAM Access Analyzer, and third-party tools ✓
- D) CloudWatch alarms and VPC flow logs

**Q173.** The automated response to a GuardDuty EC2 finding (e.g. CryptoCurrency:EC2) should be:
- A) Stop the EC2 instance immediately
- B) GuardDuty → EventBridge rule → Lambda → quarantine SG (remove all egress) + snapshot ✓
- C) Delete the instance and replace with a clean AMI
- D) Send an SNS email notification and wait for manual remediation

**Q174.** IAM `aws:RequestedRegion` condition is used to:
- A) Restrict which EC2 regions can assume a role
- B) Restrict API calls to approved AWS regions ✓
- C) Allow access only from specific VPC regions
- D) Limit IAM policies to the current region only

**Q175.** When a high-privilege AWS access key is found in a public GitHub repo, the FIRST action is:
- A) Audit CloudTrail for the past 90 days
- B) Deactivate the key in IAM immediately ✓
- C) Delete the GitHub repository
- D) Change the AWS account root password

**Q176.** AWS Inspector v2 scans for:
- A) S3 data classification and PII detection
- B) Network traffic anomalies in VPC flow logs
- C) EC2/Lambda/container software vulnerabilities (CVEs) and network reachability ✓
- D) IAM permission misconfigurations

**Q177.** S3 Object Lock with Compliance mode means:
- A) Only the bucket owner can delete objects
- B) No one (including root) can delete or overwrite protected objects during the retention period ✓
- C) Objects can be deleted with MFA delete enabled
- D) Objects are encrypted but can be overwritten

**Q178.** IAM Access Analyzer identifies:
- A) Unused IAM users and roles
- B) Resources accessible from outside the account or organization ✓
- C) IAM policy syntax errors
- D) EC2 instance metadata service misconfigurations

**Q179.** Server-Side Encryption with Customer-Managed Keys (SSE-KMS) vs SSE-S3:
- A) SSE-KMS is cheaper and simpler to manage
- B) SSE-KMS provides audit trail (CloudTrail) and key policy control; SSE-S3 does not ✓
- C) SSE-S3 provides stronger encryption than SSE-KMS
- D) SSE-KMS requires the client to manage encryption keys locally

**Q180.** AWS CloudTrail data events for S3 vs management events differ in that:
- A) Data events record S3 API calls (GetObject, PutObject) and are disabled by default ✓
- B) Management events record S3 object operations like PutObject
- C) Data events are free and management events are paid
- D) CloudTrail only supports management events for S3

**Section J-K answer key:** 156-B, 157-B, 158-C, 159-A, 160-B, 161-B, 162-C, 163-B, 164-B, 165-B, 166-B, 167-B, 168-C, 169-B, 170-B, 171-B, 172-C, 173-B, 174-B, 175-B, 176-C, 177-B, 178-B, 179-B, 180-A

---

## Section L-M — DevOps & SaaS/Multi-Tenant (Q181–200)

**Q181.** GitOps differs from traditional push-based CI/CD in that:
- A) GitOps uses Jenkins while CI/CD uses CodePipeline
- B) In GitOps, an operator (e.g., ArgoCD) pulls desired state from Git and applies it ✓
- C) GitOps applies infra changes on every commit without approval gates
- D) GitOps only works for Kubernetes, not ECS

**Q182.** A CodeDeploy canary deployment for Lambda:
- A) Shifts all traffic instantly, then rolls back if alarms fire
- B) Routes a small percentage of traffic to the new version, incrementally increasing it ✓
- C) Uses Route 53 weighted records to split traffic
- D) Only works with Lambda functions under 10 MB

**Q183.** The `bake time` in a blue/green deployment refers to:
- A) The time to build and push the Docker image
- B) The period after full traffic switch during which the old (blue) environment is kept for rollback ✓
- C) The time CloudFormation takes to apply the change set
- D) The health check grace period before the ALB routes traffic

**Q184.** OIDC authentication from GitHub Actions to AWS eliminates the need for:
- A) An IAM role in the AWS account
- B) An IAM policy with specific permissions
- C) Static AWS access keys stored as GitHub secrets ✓
- D) AWS CLI in the GitHub Actions runner

**Q185.** AWS AppConfig is used to:
- A) Deploy application code to ECS Fargate
- B) Manage CloudFormation stack configurations
- C) Deliver feature flags and dynamic config to applications, with validation and gradual rollout ✓
- D) Store Terraform variable definitions

**Q186.** ECR image scanning on push uses which scanner?
- A) AWS Inspector v2 ✓
- B) AWS GuardDuty
- C) AWS Macie
- D) AWS Security Hub

**Q187.** CodeDeploy auto-rollback triggers can be set based on:
- A) CPU alarms on the new deployment's EC2 instances
- B) CloudWatch alarm state (ALARM) for specified alarms ✓
- C) Failed health checks on more than 50% of instances
- D) The number of rejected HTTP requests exceeding a threshold

**Q188.** In the SaaS silo model, each tenant receives:
- A) Shared ECS cluster but dedicated RDS instance
- B) Dedicated AWS account, VPC, and database — fully isolated resources ✓
- C) Dedicated schema in a shared database
- D) Dedicated subdomain routing to a shared backend

**Q189.** Noisy-neighbour tenant in a SaaS pool model is MOST effectively mitigated by:
- A) Scaling the shared ECS cluster by 50% headroom
- B) Running heavy tenant operations asynchronously via per-tenant SQS queues ✓
- C) Giving all tenants equal CloudWatch dashboards
- D) Enabling ElastiCache write-through caching for all tenants

**Q190.** JWT claim-based tenant routing means:
- A) The client sends the tenant ID in the URL path; the server trusts it
- B) The server extracts the tenant ID from the signed JWT; never trusts client-supplied headers ✓
- C) API Gateway uses custom authorizers to add a tenant header
- D) CloudFront reads the cookie and adds a tenant ID header

**Q191.** In schema-per-tenant PostgreSQL, a database migration must:
- A) Run once on a global schema (shared across tenants)
- B) Run separately for each tenant's schema (N schemas × 1 migration) ✓
- C) Run only when a new tenant is onboarded
- D) Use RDS native schema migration tools

**Q192.** Row-Level Security (RLS) in PostgreSQL prevents cross-tenant data leakage by:
- A) Encrypting each tenant's rows with a unique key
- B) Automatically adding `WHERE tenant_id = current_setting('app.tenant_id')` to every query ✓
- C) Creating separate database users per tenant
- D) Isolating each tenant to a separate table

**Q193.** Cryptographic erasure for GDPR compliance in a SaaS pool means:
- A) Deleting all rows with the tenant_id in the database
- B) Encrypting each tenant's data with a unique KMS CMK and deleting the CMK to make data unreadable ✓
- C) Exporting tenant data to a compliant archive before deletion
- D) Overwriting all tenant records with null values

**Q194.** A SaaS control plane (vs data plane) handles:
- A) Processing payroll and leave requests for all tenants
- B) Tenant registration, billing, user pools, and account provisioning ✓
- C) Routing API requests to the correct tenant's database
- D) Scaling ECS tasks based on tenant workload

**Q195.** The bridge tenancy model allows:
- A) Tenants to switch between silo and pool by contacting AWS support
- B) Free/Standard tenants on a shared pool and Enterprise tenants on dedicated silo infrastructure ✓
- C) Two tenants to share a VPC for cost savings
- D) Pool tenants to access silo tenants' resources for collaboration

**Q196.** To monitor per-tenant performance in CloudWatch:
- A) Create a separate AWS account per tenant with individual dashboards
- B) Add `TenantId` as a custom CloudWatch metric dimension ✓
- C) Use X-Ray to trace requests, grouping by tenant name
- D) Enable CloudWatch agent with tenant-specific config files

**Q197.** AWS Marketplace Metering Service is used in SaaS to:
- A) Monitor CPU and memory usage for billing
- B) Report per-tenant usage to AWS for Marketplace-listed SaaS billing ✓
- C) Automatically scale SaaS infrastructure based on tenant count
- D) Generate usage reports for tenants

**Q198.** Tenant onboarding automation using Step Functions should target which maximum time for pool-tier provisioning?
- A) Immediately (< 1 second)
- B) < 5 minutes ✓
- C) < 1 hour
- D) < 24 hours (next business day)

**Q199.** The "tenant tiering" design pattern classifies tenants into tiers because:
- A) AWS Organizations requires different tiers for billing purposes
- B) Different tenants have different SLA needs, compute requirements, and price points, requiring different infrastructure levels ✓
- C) Tier assignment is required by HIPAA compliance
- D) CloudFront CDN pricing is based on tenant tier

**Q200.** At 10,000 tenants in a pool SaaS, the MOST important cost optimisation is:
- A) Using EC2 Reserved Instances for the shared ECS cluster
- B) Moving all tenants to a silo model for better resource utilisation
- C) Maximising CloudFront cache-hit ratio to reduce compute and DB load ✓
- D) Replacing RDS with DynamoDB for all tenant data

**Section L-M answer key:** 181-B, 182-B, 183-B, 184-C, 185-C, 186-A, 187-B, 188-B, 189-B, 190-B, 191-B, 192-B, 193-B, 194-B, 195-B, 196-B, 197-B, 198-B, 199-B, 200-C

---

## Complete Answer Key

| Q | A | Q | A | Q | A | Q | A | Q | A |
|---|---|---|---|---|---|---|---|---|---|
| 1 | C | 41 | A | 81 | B | 121 | B | 161 | B |
| 2 | B | 42 | C | 82 | B | 122 | C | 162 | C |
| 3 | B | 43 | B | 83 | C | 123 | B | 163 | B |
| 4 | C | 44 | C | 84 | B | 124 | B | 164 | B |
| 5 | C | 45 | B | 85 | B | 125 | C | 165 | B |
| 6 | B | 46 | B | 86 | A | 126 | C | 166 | B |
| 7 | B | 47 | B | 87 | B | 127 | C | 167 | B |
| 8 | C | 48 | A | 88 | C | 128 | B | 168 | C |
| 9 | A | 49 | A | 89 | C | 129 | B | 169 | B |
| 10 | C | 50 | B | 90 | B | 130 | B | 170 | B |
| 11 | B | 51 | B | 91 | B | 131 | B | 171 | B |
| 12 | B | 52 | C | 92 | B | 132 | B | 172 | C |
| 13 | B | 53 | B | 93 | B | 133 | B | 173 | B |
| 14 | C | 54 | B | 94 | C | 134 | B | 174 | B |
| 15 | B | 55 | B | 95 | B | 135 | B | 175 | B |
| 16 | C | 56 | B | 96 | B | 136 | B | 176 | C |
| 17 | B | 57 | C | 97 | B | 137 | B | 177 | B |
| 18 | B | 58 | B | 98 | B | 138 | B | 178 | B |
| 19 | B | 59 | A | 99 | B | 139 | B | 179 | B |
| 20 | C | 60 | B | 100 | B | 140 | B | 180 | A |
| 21 | C | 61 | B | 101 | B | 141 | C | 181 | B |
| 22 | C | 62 | C | 102 | B | 142 | B | 182 | B |
| 23 | D | 63 | B | 103 | B | 143 | C | 183 | B |
| 24 | B | 64 | C | 104 | B | 144 | A | 184 | C |
| 25 | C | 65 | B | 105 | B | 145 | B | 185 | C |
| 26 | B | 66 | B | 106 | C | 146 | B | 186 | A |
| 27 | A | 67 | C | 107 | B | 147 | C | 187 | B |
| 28 | B | 68 | B | 108 | C | 148 | B | 188 | B |
| 29 | C | 69 | B | 109 | B | 149 | B | 189 | B |
| 30 | C | 70 | B | 110 | B | 150 | B | 190 | B |
| 31 | B | 71 | C | 111 | B | 151 | A | 191 | B |
| 32 | B | 72 | B | 112 | C | 152 | A | 192 | B |
| 33 | B | 73 | B | 113 | B | 153 | C | 193 | B |
| 34 | C | 74 | C | 114 | B | 154 | B | 194 | B |
| 35 | B | 75 | B | 115 | B | 155 | B | 195 | B |
| 36 | B | 76 | A | 116 | B | 156 | B | 196 | B |
| 37 | B | 77 | A | 117 | B | 157 | B | 197 | B |
| 38 | C | 78 | C | 118 | B | 158 | C | 198 | B |
| 39 | B | 79 | B | 119 | C | 159 | A | 199 | B |
| 40 | B | 80 | B | 120 | B | 160 | B | 200 | C |

---

*Pair these MCQs with the [200 Interview Questions](16-200-interview-questions.md) and validate your understanding with the [Enterprise Case Studies](14-enterprise-case-studies.md).*
